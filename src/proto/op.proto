syntax = "proto3";

package ithos;

import "object.proto";

// Ops make modifications to the state of the database
message Op {
  enum Type {
    ADD = 0;
  }

  Type optype   = 1;
  string path   = 2;
  Object object = 3;
}
