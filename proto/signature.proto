syntax = "proto3";

package ithos;

import "algorithm.proto";

// An individual signature on a given block
message Signature {
    SignatureAlgorithm algorithm = 1;
    bytes public_key = 2;
    bytes value = 3;
}
