language: rust
sudo: false

branches:
  only:
    - master

rust:
  - stable
  - beta
  - nightly

os:
  - linux
  - osx

matrix:
  fast_finish: true

notifications:
  irc: 'irc.freenode.org#cryptosphere'
