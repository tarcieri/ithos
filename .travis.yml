language: rust
sudo: false

branches:
  only:
    - master

rust:
  - nightly

os:
  - linux
  - osx

matrix:
  fast_finish: true

notifications:
  irc: 'irc.freenode.org#cryptosphere'
  webhooks: http://iqulus.cryptosphere.org:10103/travis
